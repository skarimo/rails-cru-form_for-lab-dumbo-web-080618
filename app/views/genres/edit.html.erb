<h1>Edit Genre Form</h1>

<%= form_for @genre do |f|%>
  <%=  f.label :genre_name %>
  <%= text_field_tag "genre[name]", params[:name], :id => 'genre[name]', value: @genre.name  %>

  <%= f.submit 'Update Genre' %>
<% end %>
